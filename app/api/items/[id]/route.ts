import { eq } from "drizzle-orm";
import { getChatGPTUser } from "@/app/chatgpt-auth";
import { getDb } from "@/db";
import { projectItems, projects } from "@/db/schema";
function isFoundryProjection(meta:string){try{return Boolean((JSON.parse(meta) as {foundrySync?:unknown}).foundrySync);}catch{return false;}}
export async function PATCH(r:Request,c:{params:Promise<{id:string}>}){const user=await getChatGPTUser();const{id}=await c.params;const[item]=await getDb().select().from(projectItems).where(eq(projectItems.id,id));const[p]=item?await getDb().select().from(projects).where(eq(projects.id,item.projectId)):[];if(!user||!p||p.ownerEmail!==user.email)return Response.json({error:"Not found"},{status:404});if(isFoundryProjection(item.meta))return Response.json({error:"Foundry 정본 투영은 Storyyard에서 직접 수정할 수 없음."},{status:409});const x=await r.json() as Partial<{title:string;body:string;meta:string}>;const updatedAt=new Date().toISOString();await getDb().update(projectItems).set({...x,updatedAt}).where(eq(projectItems.id,id));await getDb().update(projects).set({contentRevision:updatedAt,updatedAt}).where(eq(projects.id,item.projectId));return Response.json({ok:true});}

export async function DELETE(_:Request,c:{params:Promise<{id:string}>}){const user=await getChatGPTUser();const{id}=await c.params;const[item]=await getDb().select().from(projectItems).where(eq(projectItems.id,id));const[p]=item?await getDb().select().from(projects).where(eq(projects.id,item.projectId)):[];if(!user||!p||p.ownerEmail!==user.email)return Response.json({error:"Not found"},{status:404});if(isFoundryProjection(item.meta))return Response.json({error:"Foundry 정본 투영은 Storyyard에서 삭제할 수 없음."},{status:409});const updatedAt=new Date().toISOString();await getDb().delete(projectItems).where(eq(projectItems.id,id));await getDb().update(projects).set({contentRevision:updatedAt,updatedAt}).where(eq(projects.id,item.projectId));return Response.json({ok:true});}
